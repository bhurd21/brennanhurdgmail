# Copy to .env and fill in. Never commit .env.
# KAMAL_REGISTRY_PASSWORD comes from macOS Keychain (not here):
#   security add-internet-password -a hurdbr -s hub.docker.com -w

# Postgres URL pointing to the DB running on the server.
# For local dev this is handled by docker-compose (not needed here).
DATABASE_URL=postgresql://postgres:lahman@172.17.0.1:5432/lahman
