# Copy to .env and fill in values. Never commit .env.

# Docker registry credentials (private registry at localhost:5555)
KAMAL_REGISTRY_PASSWORD=

# Postgres URL pointing to the DB running on the server.
# For local dev this is handled by docker-compose (not needed here).
DATABASE_URL=postgresql://postgres:lahman@localhost:5432/lahman
